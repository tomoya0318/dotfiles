あなたは計画の敵対検証を行う fresh な codex です。

## Inputs

- `<work-dir>/plan.md`
- リポジトリルート
- リポジトリルートの `.claude/impl-workflow.md`（存在しなければ特別な重点領域なしで進める）
plan.md と対象コードを読み、修正はせず指摘だけを返してください。
前提の誤り、見落とし、より単純な代替案、推測で埋めた箇所、スコープの過不足を探してください。
`判断` と `方針` の振り分けが妥当かを検証してください。
技術的に決まることを `判断` に置くと accept の回数が膨れ、人間の好みが絡むことを `方針` に置くと人間が選ぶ機会を失います。
各指摘に file:line と確信度（高・中・低）を付けてください。
.claude/impl-workflow.md のレビュー重点領域があれば考慮してください。
コードについての指摘は `file:line`、計画自体についての指摘は `plan.md` の節名を位置として示してください。
